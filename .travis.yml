sudo: required
language: generic
services:
  - docker
script:
  - git clone https://github.com/aboul3la/Sublist3r.git
  - docker build -t httpse .
  - docker run --rm -ti -v $(pwd):/opt httpse bash -c "chmod +x ct.sh && ./ct.sh arb.ru"
  - git diff
