sudo: required
language: generic
services:
  - docker
script:
  - docker build -t httpse .
  - docker run --rm -ti -v $(pwd):/opt httpse bash -c "chmod +x setup.sh && ./setup.sh && python3 old-ruleset-generator.py -d arb.ru"
